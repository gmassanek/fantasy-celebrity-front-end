// import Ember from 'ember';

export function initialize(/* container, application */) {
  // Ember.deprecate = function(){};
  // Ember.warn = function(i){};
}

export default {
  name: 'hide-deprecations',
  initialize: initialize
};
